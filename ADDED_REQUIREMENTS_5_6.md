# إضافات المتطلبين 5 و6 (محدّثة حسب الإعلان التوضيحي)

## المتطلب 6 — الواجهات والتنقل
- تمت إضافة واجهة رابعة كانت ناقصة: **حذف منتج** (`/delete/<id>` في `tests/flask/app.py` + قالب `delete_product.html` جديد بصفحة تأكيد Yes/Cancel).
- شريط تنقل (navbar) موحّد في الواجهات الأربع كلها (المنتجات، إضافة منتج، تفاصيل منتج، حذف منتج) للتنقل السلس بينها.
- **CSS منفصل**: تمت إزالة `<style>` الداخلية من كل القوالب، واستُبدلت بملف واحد مشترك `tests/flask/static/style.css` (مقسّم بأقسام: مشترك + قسم لكل صفحة عبر كلاس على `<body>` مثل `page-index`, `page-add`, `page-detail`, `page-delete` لتفادي أي تعارض بين أسماء الأصناف). القوالب تربطه بـ `<link rel="stylesheet" href="/static/style.css">`.
- تم اختبار كل السيناريوهات فعلياً عبر Flask test client (عرض، إضافة، تفاصيل، حذف، تحميل ملف style.css، منتج غير موجود) واشتغلت كلها بنجاح.

## المتطلب 5 — توليد الكود (مطابق الآن لمخطط الإعلان)
`Main.generateWebsite()` (يُشغَّل عبر `java Main --generate` أو `-g`) ينفّذ المخطط بالضبط:

```
app.py → Python Parser → Python AST → Semantic Analysis → Generator
       → Context Data → render_template() → Jinja Parser → Jinja AST
       → تبديل المتغيرات → HTML → المتصفح
```

### مجلد `output/` (الخرج الحقيقي + الملفات المرافقة كما هي)
- `index.html` ← مُولَّد من `products.html` (شجرة Jinja) + بيانات `products` (شجرة Python)
- `add_product.html` ← مُولَّد (نموذج فارغ، لا يحتاج بيانات)
- `product_detail.html` ← مُولَّد (يُمرَّر له أول منتج كـ `product` مفرد)
- `delete_product.html` ← مُولَّد (نفس الفكرة، مع صفحة تأكيد)
- `app.py` و`style.css` ← **نُسخا كما هما دون أي معالجة** (ملفات داعمة فقط، لا تدخل في التحليل/التوليد)
- ملاحظة: لا يوجد `script.js` في هذا المشروع أصلاً (وهو اختياري حسب الإعلان)، فلم يُنسخ.

> **بخصوص التسمية**: مثال الإعلان استخدم `index/add_product/edit_product` كتوضيح عام للآلية. مشروعنا الفعلي مطلوب فيه 4 واجهات محددة سلفاً (عرض، إضافة، **تفاصيل**، **حذف**) وليس "تعديل"، لذلك سمّيت الصفحة الرئيسية `index.html` لمطابقة الاصطلاح، وأبقيت `product_detail.html` و`delete_product.html` بأسمائهما الحقيقية بدل تسميتها زوراً "edit" لأنو ما في وظيفة تعديل أصلاً بالمشروع. لو أستاذة المادة قصدت تحديدًا اسم "edit_product" لازم يكون موجود، خبريني وبعدلها.

### مجلد `compiler_output/` (نواتج التحليل والتوليد)
- `ast_python.json` — شجرة AST لملف `app.py` بصيغة JSON (اسم العقدة، السطر، العمود، معلومات إضافية، الأبناء بشكل متكرر).
- `ast_jinja.json` — مصفوفة JSON تحوي شجرة AST لكل قالب من القوالب الأربعة (مع اسم الملف).
- `semantic_report.txt` — تقرير التحليل الدلالي لكل ملف (app.py + كل قالب): عدد الأخطاء الدلالية ونصّها إن وجدت.
- `generation_log.txt` — سجل زمني كامل لخطوات التوليد (بداية/نهاية، أي ملف نجح أو فشل ولماذا، أي ملفات نُسخت).

## كيف تجرّب المشروع

### أ) تجربة الواجهات (متطلب 6) — لا يحتاج Java
```bash
cd tests/flask
pip install flask
python app.py
```
افتح `http://localhost:8080` وجرّب: عرض المنتجات، إضافة منتج، تفاصيل منتج، حذف منتج، والتنقل بينها عبر الشريط العلوي.

### ب) تجربة المترجم والتوليد (المتطلبين 5 و6 كاملين)
افتح المشروع بـ IntelliJ (فيه `.idea` أصلاً) مع `dependencies/antlr-4.13.2-complete.jar` على classpath، ثم:
```bash
java Main            # المراحل 1-4 على كل الاختبارات
java Main -g          # يولّد output/ و compiler_output/ كاملين
```
افتح `output/index.html` مباشرة بالمتصفح لترى نتيجة التوليد الساكنة، وراجع ملفات `compiler_output/*.json` و`*.txt` لرؤية نواتج التحليل.

> ملاحظة: ما قدرت أصرّف الكود Java هنا لعدم توفر `javac` ببيئة التنفيذ، بس فحصت توازن الأقواس بعناية ومطابقة النمط البرمجي الأصلي. صرّفيه أول شي عندك وقوليلي لو طلع أي error بسيط لأصلحه فوراً.
